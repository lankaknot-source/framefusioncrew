rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app currently has no user login screen.
    // For production, add Firebase Authentication and replace this rule
    // with an authenticated/admin-only rule.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
